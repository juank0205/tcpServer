{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "codelldb",
            "request": "launch",
            "name": "C++",
            "preLaunchTask": "Compile",
            "expressions": "native",
            "program": "${workspaceFolder}/bin/Linux64/${fileBasenameNoExtension}"
        }
    ]
}
